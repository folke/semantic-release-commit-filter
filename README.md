# :rocket: Semantic-Release Commit Filter

This package is both a plugin and shareable configuration for semantic-release.

It does two things:
* configure `tagFormat = "${package.name}-v${version}"`
* only use `commits`